# [BACKEND] Environment Variables

# Database (AlwaysData MySQL)
DATABASE_URL="mysql://433914:17112015Jv*@mysql-cantinaonline.alwaysdata.net/cantinaonline_tcc?sslaccept=strict&connection_limit=5"

# JWT Secrets (mude para valores fortes em produção)
JWT_SECRET="troque_esta_chave_para_algo_forte_em_producao"
REFRESH_JWT_SECRET="troque_esta_chave_refresh_para_algo_forte_em_producao"

# Token TTL
ACCESS_TTL="15m"
REFRESH_TTL="7d"

# [PRODUCTION] CORS Origins (múltiplos, separados por vírgula)
CORS_ORIGIN="https://cantina-online.vercel.app,https://api.seu-dominio.com"

# Frontend URL (para links de reset password, etc)
FRONTEND_URL="https://cantina-online.vercel.app"

# [PRODUCTION] Node Environment
NODE_ENV="production"

# Port (fornecido pelo host em produção, padrão 4000)
PORT="4000"

