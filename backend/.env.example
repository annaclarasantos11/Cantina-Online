# Database
DATABASE_URL="mysql://user:password@host/database"

# JWT Secrets
JWT_SECRET="your-secret-key-here"
REFRESH_JWT_SECRET="your-refresh-secret-key-here"

# Token TTL
ACCESS_TTL="15m"
REFRESH_TTL="7d"

# CORS and URLs
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Email Service (AWS SES)
# Para configurar AWS SES:
# 1. Crie uma conta AWS (https://aws.amazon.com)
# 2. Acesse AWS Console > Simple Email Service (SES)
# 3. Verifique seu domínio ou e-mail de origem
# 4. Crie credentials IAM com permissão "AmazonSESFullAccess"
# 5. Copie os valores abaixo
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="sua-access-key-id"
AWS_SECRET_ACCESS_KEY="sua-secret-access-key"
AWS_SES_FROM_EMAIL="seu-email-verificado@seu-dominio.com"

# Server
PORT=4000
NODE_ENV=development
