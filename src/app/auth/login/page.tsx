import LoginClient from "./LoginClient";

export const metadata = {
  title: "Entrar — Cantina Online",
  description: "Acesse sua conta para fazer pedidos.",
};

export default function LoginPage() {
  return <LoginClient />;
}
